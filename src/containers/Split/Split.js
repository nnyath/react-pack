import React from 'react'

export default class Split extends React.Component {
  render () {
    return (
      <div>
        If you check your Network graph in the dev console, you should see another JS file load! Success! Or if you run dist, there should be another JS file!
      </div>
    )
  }
}
